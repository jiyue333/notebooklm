meta {
  name: Update Theme Only
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/settings
  body: none
  auth: bearer
}

headers {
  content-type: application/json
}

auth:bearer {
  token: {{authToken}}
}

body {
  {
    "themeColor": "forest",
    "colorMode": "dark"
  }
}
