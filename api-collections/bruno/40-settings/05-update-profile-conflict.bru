meta {
  name: Update Profile Conflict
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/account/profile
  body: none
  auth: bearer
}

headers {
  content-type: application/json
}

auth:bearer {
  token: {{authToken}}
}

body {
  {
    "username": "{{noKeyUsername}}"
  }
}
