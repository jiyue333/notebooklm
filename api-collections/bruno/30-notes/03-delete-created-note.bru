meta {
  name: Delete Created Note
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/notebooks/{{notebookMixedId}}/notes/{{createdNoteId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}
