meta {
  name: Get Completed Search Session
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/notebooks/{{notebookMixedId}}/search-sessions/{{completedSearchSessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}
