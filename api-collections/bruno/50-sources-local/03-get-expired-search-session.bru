meta {
  name: Get Expired Search Session
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/notebooks/{{notebookMixedId}}/search-sessions/{{expiredSearchSessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}
