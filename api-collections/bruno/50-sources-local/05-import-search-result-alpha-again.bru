meta {
  name: Import Search Result Alpha Again
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/notebooks/{{notebookMixedId}}/sources/import
  body: none
  auth: bearer
}

headers {
  content-type: application/json
}

auth:bearer {
  token: {{authToken}}
}

body {
  {
    "searchSessionId": "{{completedSearchSessionId}}",
    "searchResultIds": ["{{searchResultAlphaId}}"]
  }
}
