meta {
  name: Summary Cache Hit
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/notebooks/{{notebookMixedId}}/articles/{{readyArticleId}}/summary
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}
