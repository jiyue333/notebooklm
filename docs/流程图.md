## Summary

```mermaid

flowchart TD

    START([调用 generate_summary]) --> HASH[计算 content_hash\n构造 redis_key / lock_key]

    HASH --> LOCK[获取生成锁\nacquire lock]

    LOCK -- "📊 summary.lock_acquired\nelapsed_ms" --> REDIS_GET

    subgraph Phase1["Phase 1 — 缓存层查找"]

        REDIS_GET[Redis GET\nget_json redis_key]

        REDIS_GET -- "📊 summary.redis_cache_check\nelapsed_ms · hit" --> REDIS_HIT{Redis 命中?}

        REDIS_HIT -- 是 & 可用 --> CACHE_RETURN([返回缓存摘要\ncached=True])

        REDIS_HIT -- 否 --> DB_CHECK[DB 精确查找\nrepo.get_cached_summary]

        DB_CHECK -- "📊 summary.db_cache_check\nelapsed_ms · hit" --> DB_HIT{DB 命中?}

        DB_HIT -- 是 & 可用 --> SET_REDIS1[写入 Redis 缓存\n_set_summary_cache]

        SET_REDIS1 --> CACHE_RETURN

        DB_HIT -- 否 & language=zh --> ZH_CHECK[DB 最新缓存 any language\nrepo.get_latest_cached_summary]

        ZH_CHECK -- "📊 summary.zh_latest_cache_check\nelapsed_ms · found" --> ZH_HIT{有可复用缓存?}

        ZH_HIT -- 需要翻译 --> TRANSLATE[LLM 翻译为中文\n_translate_to_chinese]

        TRANSLATE -- "📊 summary.zh_translate_done\nelapsed_ms" --> ZH_DONE

        ZH_HIT -- 已是中文 --> ZH_DONE[写入 Redis 缓存]

        ZH_DONE --> CACHE_RETURN

        ZH_HIT -- 无可用缓存 --> LANGGRAPH_START

        DB_HIT -- 否 & language≠zh --> LANGGRAPH_START

    end

    subgraph Phase2["Phase 2 — LangGraph 执行 (timeout=150s)"]

        LANGGRAPH_START -- "📊 summary.langgraph_start\ncontent_chars · language" --> N1

        N1["Node 1: analyze_content\n• 文章类型识别\n• token估算 / code_ratio\n• 选 model_tier & strategy"]

        N1 -- "📊 summary.node_analyze_content\nelapsed_ms · article_type\nmodel_tier · summary_strategy" --> N2

        N2["Node 2: compress_content\n• DB 压缩缓存读取\n• compress_content() CPU\n• DB 压缩缓存写入"]

        N2 -- "📊 summary.compress_db_read\n📊 summary.compress_cpu\n📊 summary.compress_db_write\n📊 summary.node_compress_content" --> ROUTE

        ROUTE{路由: strategy?}

        ROUTE -- direct --> N3_DIRECT

        ROUTE -- map_reduce --> N4_SPLIT

        N3_DIRECT["Node 3: direct_summarize\n• 构造 System+User prompt\n• *invoke*model()"]

        N3_DIRECT -- "📊 summary.llm_direct_done\nelapsed_ms · tokens · chars\n└─ 📊 summary.llm_stream_done (streaming)\n└─ 📊 summary.llm_invoke_done (non-streaming)" --> N7_VAL

        N4_SPLIT["Node 4: map_split\n• 按标题切块 (≤8块)"]

        N4_SPLIT -- "📊 summary.node_map_split\nelapsed_ms · chunk_count" --> N5_MAP

        N5_MAP["Node 5: map_summarize ×N\n(并行 fan-out)\n• lite_llm 对每个 chunk 摘要"]

        N5_MAP -- "📊 summary.llm_map_done\nelapsed_ms · tokens · chars\n└─ 📊 summary.llm_invoke_done" --> N6_REDUCE

        N6_REDUCE["Node 6: reduce_summarize\n• 合并 chunk_summaries\n• 主模型 reduce"]

        N6_REDUCE -- "📊 summary.llm_reduce_done\nelapsed_ms · tokens · chars\n└─ 📊 summary.llm_stream_done (streaming)\n└─ 📊 summary.llm_invoke_done (non-streaming)" --> N7_VAL

        N7_VAL["Node 7: validate_summary\n• 启发式验证 (overlap ≥ 0.42 直接通过)\n• lite_llm JSON 质量校验"]

        N7_VAL -- "📊 summary.validate_heuristic_done\nelapsed_ms · overlap · issues\n📊 summary.llm_validate_done\nelapsed_ms · tokens" --> VAL_ROUTE

        VAL_ROUTE{validation_passed?}

        VAL_ROUTE -- 通过 / 超重试 --> GRAPH_END([graph.ainvoke 返回])

        VAL_ROUTE -- 不通过 & retry_count<max --> RETRY_ROUTER[retry_router]

        RETRY_ROUTER -- map_reduce --> N6_REDUCE

        RETRY_ROUTER -- direct --> N3_DIRECT

    end

    GRAPH_END -- "📊 summary.generated ★\nelapsed_ms · article_type\nvalidated · fallback_used" --> UNUSABLE{摘要可用?}

    subgraph Phase3["Phase 3 — 结果持久化"]

        UNUSABLE -- 可用 --> DB_SAVE[DB 写入 summary_cache\[nrepo.save](http://nrepo.save)_summary_cache]

        DB_SAVE -- "📊 summary.db_cache_save\nelapsed_ms" --> REDIS_SAVE[Redis SET\n_set_summary_cache]

        REDIS_SAVE -- "📊 summary.redis_cache_save\nelapsed_ms" --> AUDIT[写入 audit log\nrepo.append_generation_audit]

        AUDIT --> RETURN([返回 summary_text\ncached=False])

        UNUSABLE -- 不可用/fallback --> AUDIT

    end

    style Phase1 fill:#e8f4fd,stroke:#5b9bd5

    style Phase2 fill:#e8f5e9,stroke:#4caf50

    style Phase3 fill:#fff8e1,stroke:#f9a825

    style CACHE_RETURN fill:#b3e5fc

    style RETURN fill:#b3e5fc

```



## Ingest

```mermaid
flowchart TD
    IN([IngestInput]) --> VAL{输入校验}
    VAL -->|"invalid_input\n缺 user_id / notebook_id"| E0[/❌ 返回错误/]
    VAL -->|通过| P1

    subgraph P1["⬛ Phase 1  接入层"]
        direction TB
        FC["fetch_content()\n TEXT → encode UTF-8\n FILE → 读 bytes / file_store\n URL  → httpx 流式下载\n       最大 200MB，5 次重定向\n       SSL 失败自动降级 no-verify"]
        FC --> DR["detect_and_route()\n Tika MIME 检测\n MIME → DocRoute\n  PDF / HTML / OFFICE / IMAGE / TEXT\n 未识别 → UnsupportedContentTypeError\n sha256 → content_hash"]
        DR --> DUP{内容去重\ncontent_hash}
    end

    DUP -->|"unsupported_content_type"| E1[/❌ 返回错误/]
    DUP -->|"is_duplicate = true"| RD[/⏭ 跳过返回 duplicate/]
    DUP -->|继续| P2

    subgraph P2["⬛ Phase 2  解析层  parse_to_markdown()"]
        direction TB
        RT{DocRoute?}
        RT -->|TEXT| TT["直通\n UTF-8 decode\n 跳过 MinerU"]
        RT -->|"URL\n有 batch_id"| EP["poll 已有 batch\nMinerU Cloud"]
        RT -->|"URL\n无 batch_id"| UB["MinerU URL batch\n熔断器 + Semaphore 限流"]
        RT -->|"FILE / PDF\nOFFICE / IMAGE"| FB["MinerU file batch\n PDF/OFFICE/IMAGE → vlm\n HTML → MinerU-HTML"]
        UB -->|失败| FBF["回退 file batch"]
        FBF -->|失败 + HTML| LH["本地降级\n正则剥 HTML 标签"]
        FBF -->|失败 + binary| LB["本地降级\nUTF-8 / Latin-1 文本抽取"]
    end

    P2 -->|"parse_empty\nparse_failed"| E2[/❌ 返回错误/]
    P2 -->|raw_markdown| P3

    subgraph P3["⬛ Phase 3  规范化层  process_markdown()"]
        direction TB
        CHK{LRU 缓存命中?}
        CHK -->|命中| CACHED["直接返回\ncache_hit"]
        CHK -->|未命中| RMK

        subgraph RMK["Node.js remark-processor\n持久子进程，加锁串行通信"]
            direction LR
            FIX["8 个修复插件\n① fixBrokenParagraphs 合并断裂段落\n② fixHeadingLevels 标题级别归一化\n③ fixListIndent 列表结构修复\n④ fixCodeBlocks 代码块闭合\n⑤ fixTables 表格列数补齐\n⑥ fixLinks 清理空 href\n⑦ compressBlankLines 压缩空行\n⑧ fixOcrSpacing CJK 间距 / 零宽字符"]
            FIX --> EXT["extractToc → TOCNode[]\nreadingTime → 中文400字/min\n               英文250词/min"]
            EXT --> HTML["rehype-slug + rehype-stringify\n渲染 HTML，标题加 id"]
        end
        RMK --> CACHE_W["写 LRU 缓存\nSHA-256 → RemarkResult"]
    end

    P3 -->|"normalize_failed\nremark 超时/崩溃 → 降级透传"| P3

    P3 --> TOC

    subgraph TOC["⬛ Phase 3b  TOC 增强  ensure_toc()"]
        direction TB
        T1{remark.toc\n已有?}
        T1 -->|是| TOK[直接使用]
        T1 -->|否| T2["正则扫 clean_markdown 标题\n_extract_toc_from_markdown()"]
        T2 -->|找到| TMD["构造 TOCNode[]\nanchor 支持 CJK slug"]
        T2 -->|未找到| T3["LLM fallback\nbuild_lite_llm()\n最多 60 项，截断 6000 字符"]
        T3 -->|成功| TLLM["解析 JSON → TOCNode[]"]
        T3 -->|失败| TMISS["toc = []"]
    end

    TOC --> FORK(("🔀 asyncio\ncreate_task"))

    FORK -->|"主协程"| P4
    FORK -->|"后台 task"| SW

    subgraph P4["⬛ Phase 4  索引层"]
        direction TB
        BC["build_chunks()\n 按字符数切分\n chunk_target_tokens × 4 chars\n 优先 \\n\\n 边界断开\n 标注 section_id / heading_title"]
        BC --> CTX["contextualize_chunks()\n 每 chunk 拼前缀\n 笔记本 > 文章 > 章节\n 开头含代词 → LLM 补 1-2 句上下文\n build_lite_llm()"]
        CTX --> EMB["embed_chunks()\n 调 Embedding API\n 分批 64 条\n chunk.embedding = vector[]"]
    end

    subgraph SW["⚡ 并行  摘要预热"]
        direction TB
        SG["warm_summary()\n generate_summary()\n timeout 30s\n 失败静默不影响主流程"]
    end

    P4 --> JOIN(("🔁 await\nsummary_task"))
    SW --> JOIN

    JOIN --> RES(["✅ IngestResult\n clean_markdown  mdast  html\n toc[]  chunks[text + embedding]\n parser_name  content_hash\n timings: 各阶段 ms 明细"])

    style P1 fill:#1e2a3a,color:#cdd9e5
    style P2 fill:#1e2a3a,color:#cdd9e5
    style P3 fill:#1e2a3a,color:#cdd9e5
    style TOC fill:#1e2a3a,color:#cdd9e5
    style P4 fill:#1e3a2a,color:#cdd9e5
    style SW fill:#2a1e3a,color:#cdd9e5
    style RMK fill:#152030,color:#aac8e0
    style FORK fill:#c47d00,color:#fff
    style JOIN fill:#005a82,color:#fff
    style RES fill:#1a6e3a,color:#fff
```

## Chat

```mermaid
flowchart TD
    CLIENT([HTTP 请求]) --> SVC

    subgraph SVC["service.py · stream_message"]
        direction TB
        S1["① build_user_chat_model\n📊 chat.init_model"]
        S2["② _prepare_context\n  - get_notebook\n  - get/create conversation\n  - get_article\n  - COUNT articles / indexed\n  - append user message\n📊 chat.prepare_context"]
        S3["③ list_recent_messages\n📊 chat.load_history"]
        S4["④ graph.ainvoke\n📊 chat.graph_start / chat.graph_done"]
        S5["⑤ yield token chunks"]
        S6["⑥ append_message (assistant)\n   _maybe_refresh_rolling_summary\n   db.commit\n📊 chat.persist"]
        S1 --> S2 --> S3 --> S4 --> S5 --> S6
    end

    SVC --> GRAPH

    subgraph GRAPH["LangGraph · ChatGraphState"]
        direction TB

        subgraph N1["Node 1: query_router"]
            QR1{规则匹配?\narticle_id / keywords}
            QR2["LLM structured_output\nRouteDecision\n📊 chat.router_llm_structured"]
            QR3["Text fallback\n📊 chat.router_llm_text_fallback"]
            QR1 -->|命中| QR_DONE["返回 route/scope/mode"]
            QR1 -->|未命中| QR2
            QR2 -->|失败| QR3
            QR3 --> QR_DONE
            QR2 --> QR_DONE
        end

        subgraph N2["Node 2: retrieval_planner"]
            RP1["根据 route/scope/article_count\n动态计算 dense/sparse/rerank 预算\n纯 CPU，无 IO"]
            RP1 --> RP2["返回 RetrievalPlanSpec\nstrategy: chunk_only /\narticle_then_chunk / hybrid / skip"]
        end

        COND{strategy == skip?}

        subgraph N3["Node 3: retrieval_engine"]
            direction TB
            RE_CO["chunk_only\n  hybrid_retrieval\n  📊 chat.retrieval_hybrid"]
            RE_ATC["article_then_chunk\n  article_recall\n  📊 chat.retrieval_article_recall\n  hybrid_retrieval\n  📊 chat.retrieval_hybrid"]
            RE_HY["hybrid\n  _get_notebook_article_ids\n  📊 chat.retrieval_scope_articles\n  hybrid_retrieval\n  📊 chat.retrieval_hybrid"]
            RE_FB["fallback evidence\n  (article summary)\n  📊 chat.retrieval_fallback_evidence"]
        end

        subgraph N4["Node 4: web_search_broker"]
            direction TB
            WB1{规则快速判定\nfreshness / insufficient_local}
            WB2["LLM WebSearchDecision\n📊 chat.web_broker_llm_decide"]
            WB3["Redis cache get\n📊 chat.web_broker_cache_check"]
            WB4{cache hit?}
            WB5["_execute_search\n  Tavily → 📊 chat.tavily_search_done\n  Exa fallback → 📊 chat.exa_search_done\n📊 chat.web_broker_search_done"]
            WB6["Redis cache set"]
            WB1 -->|need=false| WB2
            WB2 -->|need=true| WB3
            WB1 -->|need=true| WB3
            WB3 --> WB4
            WB4 -->|hit| WB_DONE["返回 web_evidence"]
            WB4 -->|miss| WB5
            WB5 --> WB6 --> WB_DONE
        end

        subgraph N5["Node 5: answer_generator"]
            AG1["build_user_chat_model\n📊 chat.answer_gen_build_model"]
            AG2["拼接 evidence + history\n构建 messages"]
            AG3["model.ainvoke\n(用户聊天模型)\n📊 chat.answer_gen_llm_done\n含 tokens / answer_len"]
            AG1 --> AG2 --> AG3
        end

        subgraph N6["Node 6: citation_verifier"]
            CV1["校验引用 ID 合法性\n_score_citation_support (token overlap)"]
            CV2["_rewrite_answer_citations\n重新编号 [n] / [Wn]"]
            CV3["构建 trace_log"]
            CV1 --> CV2 --> CV3
        end

        N1 --> N2 --> COND
        COND -->|retrieve| N3
        COND -->|skip| N4
        N3 --> N4
        N4 --> N5
        N5 --> N6

        N1 -.->|timeout/error\n_fallback_update| N2
        N2 -.->|timeout/error| N3
        N3 -.->|timeout/error| N4
        N4 -.->|timeout/error| N5
        N5 -.->|timeout/error| N6
    end

    GRAPH --> SVC

    subgraph TIMING["关键耗时锚点（structlog key）"]
        direction LR
        T1["chat.prepare_context\nchat.load_history"]
        T2["chat.graph_start\nchat.graph_done\nchat.node_start\nchat.node_done\nchat.node_timeout"]
        T3["chat.router_llm_structured\nchat.router_llm_text_fallback"]
        T4["chat.retrieval_article_recall\nchat.retrieval_scope_articles\nchat.retrieval_hybrid\nchat.retrieval_fallback_evidence"]
        T5["chat.web_broker_llm_decide\nchat.web_broker_cache_check\nchat.tavily_search_done\nchat.exa_search_done"]
        T6["chat.answer_gen_build_model\nchat.answer_gen_context\nchat.answer_gen_llm_done"]
        T7["chat.persist\nchat.rolling_summary\nchat.completed"]
    end

```


