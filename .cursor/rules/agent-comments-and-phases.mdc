---
description: 统一中文注释与编排文件 phase/step 分隔格式
alwaysApply: true
---

# 注释与分段格式

- 注释统一使用简体中文，避免中英混杂；没有必要时尽量少写注释。
- 优先写说明“为什么”的简短注释，不写复述代码字面含义的注释。
- 对 `graph.py`、`service.py` 这类编排文件，优先用阶段分割注释组织代码。

```python
# ========== phase 1 加载上下文 ==========
# ====== step 1 获取会话 ======
# ====== step 2 读取历史 ======
```

- `phase` 用于较大流程块，`step` 用于 phase 内的关键步骤。
- 如果文件已经有分段注释，继续沿用上述格式，不要混用其他分隔样式。
